[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py create_admin && gunicorn sweet60_project.wsgi --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
